- [x] Update `PaiNN_Architecture.hpp`
  - [x] Add `m_use_bias` and `m_toxvaerd_alpha` parameters to network components.
  - [x] Configure `filter_mlp` to respect `m_use_bias`.
  - [x] Implement Toxvaerd C4 cutoff function.
  - [x] Apply Toxvaerd cutoff in `expansion_rbf` and as envelope.
- [x] Update `train_painn.cpp`
  - [x] Parse `use_bias` and `toxvaerd_alpha` from JSON.
  - [x] Instantiate `PaiNNModel` with new parameters.
- [x] Update C++ / Python Integration (`espresso`)
  - [x] Update `PaiNN_ML_Potential.hpp` and `.cpp` signatures.
  - [x] Update Cython bindings (`painn.pyx`).
- [x] Update Python runner scripts (`equilibrate.py`, `run_cg_md.py`)
  - [x] Add CLI arguments for `use_bias` and `toxvaerd_alpha`.
  - [x] Pass parameters to the potential activation function.
- [ ] Recompile Training Framework and ESPResSo.
- [ ] Validation
  - [ ] Update `tel22_training_config.json` with new options.
  - [ ] Retrain model (run `03_train_model.sh`).
  - [ ] Run `04_equilibrate.sh`
  - [ ] Run `run_nve_scaling_envelope.py` to confirm optimal Verlet scaling.
